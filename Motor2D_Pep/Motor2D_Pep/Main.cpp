#include "Point2D.h"
#include "String.h"
#include "Dlist.h"
#include "DynArray.h"

int main(int argc, char** argv)
{
	return (0);
}

//factorial (!4 = 24)
int Factorial(int a)
{
	return(a > 1) ? a = Factorial(a - 1) : 1;
}